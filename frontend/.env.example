# GitHub Memory System Frontend Configuration

# Frontend port (change if needed to avoid conflicts)
PORT=8745

# API endpoint - should point to your RefactorForge backend server
REACT_APP_API_URL=http://localhost:8001

# Development settings
GENERATE_SOURCEMAP=false

# Data source configuration (set to true for production API)
REACT_APP_USE_REAL_DATA=true